<section class="hero">
    <figure>
        <img
            src="{{ Vite::asset($imgSrc) }}"
            alt="{{ $imgAlt }}"
        >
        <figcaption>{{ $imageCaption }}</figcaption>
    </figure>
    <h1 class="glossy-text">{{ $title }}</h1>
</section>
